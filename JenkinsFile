pipeline{
    agent any

    stages{
        
        stage("Build"){
            steps{
                sh "pip install playsound"
                sh "python3 -u alarm.py"
            }
        }
    }
}