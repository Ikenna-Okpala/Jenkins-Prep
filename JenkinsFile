pipeline{
    agent any

    stages{
        
        stage("Build"){
            steps{
                sh "pip install playsound"
                sh "python -u alarm.py"
            }
        }
    }
}